// Fonts are self-hosted via @fontsource (imported in index.css) so that
// rendering never depends on a live network call to Google Fonts.
export const outfitFontFamily = "Outfit";
export const spaceMonoFontFamily = "Space Mono";
